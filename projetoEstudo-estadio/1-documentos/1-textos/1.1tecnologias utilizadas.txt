Lista final consolidada
Back-end

Node.js + TypeScript
Express (ou Fastify) como framework HTTP
Sequelize — ORM
JWT — autenticação
Redis — cache e reserva temporária (TTL/locks)

Front-end

Vue 3 (Composition API)
Quasar — framework de UI
Pinia — gerenciamento de estado
Vue Router — roteamento

Banco de dados

PostgreSQL

Pagamento

Mercado Pago (ou Stripe/PagSeguro)

Ingressos / Check-in

qrcode (Node) — geração
html5-qrcode (front) — leitura/validação

Infraestrutura

Docker + Docker Compose (Postgres + Redis)
Nodemailer ou SendGrid — e-mails
Jest ou Vitest — testes

